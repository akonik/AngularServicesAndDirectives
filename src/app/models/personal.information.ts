export class PersonalData {
    public name: string;
    public summary: string;

    constructor(name: string,
        summary: string) {
            this.name = name;
            this.summary = summary;

    }
}